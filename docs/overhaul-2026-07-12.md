# Overhaul do vertical slice — auditoria de 2026-07-12

> **FECHAMENTO (mesmo dia).** Todos os achados `[corrigir]` e `[corrigir-legibilidade]`
> foram aplicados nos commits dos lotes C1–C3 (prosa) e da Fase D (UI). O pipeline
> `revisar-prosa` rodou sobre o diff acumulado: o editor-crítico e o fiscal apontaram
> 2 bloqueantes novos nas reescritas (vozMestre de `dep_dividas` concluindo pelo
> jogador; parágrafo da cena afirmando localização falsa dos vestígios) — ambos
> corrigidos com as reescritas mínimas dos próprios revisores, mais 1 alto (fregueses
> confirmando "um minuto sequer" contra o fecho das dez) e os menores acatados
> (teste do livor em dois gestos, "lascas" do verniz, monólogo sem presumir álibis
> colhidos, carimbo de Blackwood uniformizado, comentários de design precisados).
> Estado final: build limpo · `qa.mjs` CASO VÁLIDO (16/16) · `qa-ui.mjs` UI VÁLIDA
> (3 rotas + flat, zero erros de console) · greps anti-padrão na prosa iguais ou
> menores que o baseline (travessões novos só em comentários de código) ·
> zero achados bloqueantes remanescentes. Pendências: seção
> "Decisões pendentes do usuário" abaixo.

Auditoria integral do caso "O Álibi de Corda" contra as bases de conhecimento gravadas
no PR #10 (`docs/kb-mundo-vitoriano/`, `docs/kb-craft-narrativo/`, `docs/kb-producao/`)
somadas às normas já vigentes (`docs/guia-de-estilo.md`, `docs/biblia-de-vozes.md`,
`docs/kb-medicina-legal/`). Baseline verde no ponto de partida (merge do PR #10):
build limpo, `qa.mjs` 16/16, `qa-ui.mjs` 3 rotas + flat.

Cada achado carrega uma AÇÃO:

- **[corrigir]** — incontroverso; aplicado nas fases C/D deste overhaul.
- **[decisão-usuário]** — exigiria mudar `seed.js`/`tagsOcultas`/motor, renomear rótulo
  contratado pelo QA sem erro grave, ou item de UI marcado `[DECISÃO DO USUÁRIO]` na KB;
  registrado na seção final para o usuário decidir.
- **[fora-de-escopo]** — pendência de campanha/procedural, não do vertical slice.

## Contrato do `qa-ui.mjs` — strings e seletores intocáveis

Renomear qualquer item abaixo exige atualizar `scripts/qa-ui.mjs` no MESMO commit
(regra do `CLAUDE.md`); neste overhaul, rename só por erro grave.

- **Botões/fluxo:** `→` (abertura), `Entrar — iniciar a investigação`, `Medir temperatura`,
  `fechar ✕`, `CONSTRUIR A ACUSAÇÃO`, `Caderneta`, `Concluir esta parte →`,
  `Levar a julgamento`, `Confirmar e julgar`, `Encerrar o caso`, `Fechar o caderno`,
  `Revisar a acusação`, `Inocente`, `Sem juízo`.
- **Causas/réus (texto exato):** `Estrangulamento por ligadura`, `Enforcamento`,
  `Edgar Arthurs`, `Sra. Mabel Hudson`.
- **`textoDisplay` clicados:** `Fibras Claras no Punho do Casaco`, `Fio de Lã Cinzenta na Gaveta`,
  `Xale de Lã Cinzenta`, `Edgar Saiu do Clube`, `Jantar no Clube Comercial`,
  `Herdeiro Único: Edgar Arthurs`, `Vizinha Jura…`, `Relógio de Lareira Esmagado`,
  `Corpo Endurecido`, `Manchas Arroxeadas`; o `qa.mjs` exige ainda `Corpo Flácido`
  (estado degradado do rigor).
- **Rótulos de nós da mesa:** `O Corpo`, `A Cena do Crime`, `A Delegacia`, `Edgar Arthurs`,
  `Sra. Hudson`, `Thomas Blackwood`, `Clube de Moorford`.
- **Âncoras/estações do mural:** `PRESENÇA — O RÉU NA CENA`, `QUANDO — A JANELA`,
  `IV · O Móbil`.
- **Selects da janela (ordem e formato):** `nth(0)` = início, `nth(1)` = fim; opções no
  formato `dia N · HHh` (ex.: `dia 13 · 20h`, `dia 14 · 08h`).
- **Classes/atributos:** `.termo-clicavel`, `.termo-extraido`, `data-overlay` na raiz de
  overlay, `svg[data-retrato]`, presença/ausência de `canvas` (3D vs `?flat=1`).
- **Asserções de conteúdo:** desfechos exatos `Vitória Absoluta` / `Erro Judiciário` /
  `Impunidade`; `· novo`; `Novo destino no mapa`; `O registro desmente o paradeiro`;
  `Epílogo`; `retrato da investiga…`; `perita` (Lenore); janela aberta com `depois de`.
- **Proibições:** `LEMBRETE DO LEGISTA` ausente do mural; nunca vazar `la_cinzenta` /
  `fibra_canhamo`; nunca `NaN`/`Infinity`; zero erros de console; nomes de perito
  `Dr. Harlan Blackwell` / `Lenore Blackwell`.

## Contagens-base dos greps `anti-padrao-ia` (Fase 0)

- P1 "não X — é Y" (inclui código/comentário): cartas 3 · glossario 1 · acusacao 1 ·
  tempo_morte 1 · veredicto 1.
- P4 travessões por arquivo (prosa): abertura 6 · cartas 13 · glossario 19 ·
  localidades 8 · monologo 16 · epilogo 6 · falaDoMestre 5.
- P9 léxico de juízo (com falsos positivos de tag/comentário): cartas 4 ·
  catalogo_causas 1 · glossario 3 · mapa 1 · rotulos 1.

Critério de fechamento (Fase E): contagens na prosa iguais ou menores.

## Achados

Sete frentes: A1 mundo vitoriano, A2 craft/fair play, A3 pipeline de prosa
(editor-crítico + perito-forense + fiscal-continuidade), A4 UI, A5 mecânica.
A5 (mecânica) devolveu **8/8 princípios PASSA, zero violações** — motor sem ids
hardcoded, mural mudo, validação só no desfecho. O perito-forense não achou
nenhum erro técnico nem anacronismo de técnica. Fair play: itens a/d/e/f/g
passam ou passam com ressalva; a falha formal fica na periferia testemunhal
(ver decisões pendentes).

### Bloqueantes — [corrigir]

| # | Onde | Achado | Norma | Correção |
|---|---|---|---|---|
| B1 | `localidades.js:77` | Hudson: "Só desci às sete, quando o Sr. Edgar já gritava no quintal" — o alarme é às 9h30; cria contradição de horário que NÃO existe nas tags (pista falsa que o motor não paga) | fiscal + `vida-cotidiana.md` âncoras | Cortar/deslocar a subordinada do grito |
| B2 | `localidades.js:91` + `cartas.js:438-440` | Taverna cheia "das oito à meia-noite" — pub rural fecha às 22h em 1893; infração de licença confessada ao delegado | `vida-cotidiana.md` §1/§8 | Casa cheia até o fecho das 22h; 22h–meia-noite coberto pelas testemunhas do fecho (contas, limpeza). Paradeiro declarado 20h–meia-noite e `tagsOcultas` INTACTOS |

### Altos — [corrigir]

| # | Onde | Achado | Correção |
|---|---|---|---|
| A-1 | `abertura.js:76`, `abertura.js:82`, `localidades.js:63` | "o senhor" cravado, quebra com a Dra. Lenore (esses trechos SÃO interpolados) | `{g:o senhor|a senhora}` |
| A-2 | `cartas.js:457` (`comp_blackwood`) | "o senhor não me fará chorar" — descricao de carta NÃO passa por `interpolar()` | Reescrever sem vocativo: "e ninguém me fará chorar" |
| A-3 | `abertura.js:59` | Título exibido "O briefing do Delegado Wycliffe" — anglicismo do séc. XX | "O relato do Delegado Wycliffe" |
| A-4 | `abertura.js:15-16` | Epigrama duplo na mesma cena (teto: 1) | Cortar a frase final ("Os mortos da Inglaterra…") |
| A-5 | `localidades.js:52-53` | Narrador ironista lê a mente de Wycliffe ("satisfeito com a sua cronologia", "já se vê com o caso fechado") — desautoriza as iscas | Ênfase volta à fala/gesto do delegado, sem glosa |
| A-6 | `cartas.js:49-51`, `:83` | Carimbo copia a descricao quase verbatim (`ev_rigor` estado 2; `ev_livores`) | Carimbos nominais próprios |
| A-7 | `falaDoMestre.js:57` | Mestre afirma livor "fixo nas costas" mesmo sem a carta de livor colhida — leitura de sinal não examinado | Condicionar ao sinal presente ou frasear agnóstico |
| A-8 | `monologo.js:138` | "O móbil que sustentei não pertencia ao acusado" — afirma o falso quando a carta é `dep_dividas` (pertence a Edgar) | "O móbil que sustentei não é o que a cadeia prova" |
| A-9 | `epilogo.js:72` | Molde universal geraria "De Sra. X…" com nome titulado | Usar helper `deQuem(n)` |
| A-10 | `glossario.js:200` | Graça em registro de laudo ("ou intactos, o que também fala") | Registro impessoal |

### Menores — [corrigir]

- `cartas.js:299` + fiscal: Gale "pouco antes de o sobrinho dar o alarme" (1h30 antes) → "na manhã em que o sobrinho deu o alarme".
- `cartas.js:438/440`: carimbo "20h–00h (13/out)" — meia-noite já é 14/out → "noite de 13, das 20h à meia-noite".
- `cartas.js:374`: descricao de Edgar ecoa a prosa da localidade → descricao só acrescenta.
- `cartas.js:102`: vozMestre "pela assinatura do catálogo" — perito de 1893 não invoca catálogo → "pela assinatura".
- `cartas.js:322` (comentário de código): "TESTEMUNHO FALSO… Mente" — Pruitt viu Hudson de verdade (furto à meia-noite); falsa é a leitura de autoria → precisar comentário.
- `cartas.js:281` (`dep_visto_vivo`): "a última vez que alguém da casa o viu com vida" — Edgar o viu às 22h; ancorar no registro ("diz o registro").
- `cartas.js:132` (`ev_fibras_sulco`): textoDisplay com 7 palavras (norma 2–6), NÃO contratado pelo QA → "Fibras Claras na Marca do Pescoço".
- `localidades.js:36-37`: `[[ev_fio_la]]` e `[[ev_lenco]]` fechando parágrafo/cena (posição de acento; "onde a vassoura não alcança" sussurra ocultação) → mover para o miolo, vizinhos de mesmo peso.
- `localidades.js:66`: mini-encenação de revelação no `[[ev_fibras_manga]]` → aplainar o gesto (mantendo-a acessível: é a pista-nexo do tutorial).
- `localidades.js:78`: "frio da cozinha" com range que ficaria aceso — mostrar o fogão apagado desde a morte do patrão, sem glosar (anomalia vira sinal).
- `abertura.js:16` × `:43`: tríade lente/termômetro/caderneta repetida → variar a do passo 1.
- `abertura.js:52`: "pálido como quem viu o primeiro morto da carreira" → "um guarda moço, pálido, o olhar pregado na cortina".
- `glossario.js:126`: "predileto dos casos domésticos" → "frequente nos envenenamentos domésticos".
- `glossario.js:50, 64`: maiúsculas de ênfase (FAIXA, FAMÍLIA) → minúsculas.
- `glossario.js:243-244`: verbete promete individualização de fibra ("a mão que o segurou") que a lente de 1893 não dá → "compatível, não exclusivo".
- `monologo.js:120, 135`: meias-máximas em texto de falha → constatação específica do caso.
- `monologo.js:93`: "Solto {réu}…" — o perito não solta ninguém → "{réu} fica solto por falta…".
- `mapa.js:30`: rótulo "casa da governanta" — a casa é da vítima → "casa do morto".
- A2#5 (fair play g): nada hierarquiza herança sobre dívidas — plantar em prosa, sem tocar motor, que a dívida é o aperto e a herança a saída (a opção de o motor aceitar ambos fica em decisão pendente).

### UI — [corrigir-legibilidade] (Fase D)

Calculados pela fórmula WCAG (mín. 4,5:1 texto normal):

1. Token `tinta.apagada` `#7c6c50` sobre pergaminho = 3,78:1 → escurecer para `#6b5c43` (4,81:1) em `tailwind.config.js:31`.
2. `.termo-extraido` `text-stone-500` sobre couro = 3,75:1 → `text-stone-400` (7,13:1) dentro da regra em `index.css` (nome da classe intocado).
3. `.placa-latao`: metade inferior do gradiente cai a 2,44:1 → comprimir gradiente para não descer de `#a3823f` (pior caso 4,75:1) na área do texto.
4. `.termo-clicavel`: alvo efetivo ~19-22px < 24px → `padding-block: 0.3em` na regra CSS (não desloca linhas).
5. Hit-area de remoção do barbante: `strokeWidth 14` → `24` (`MuralAcusacao.jsx:888-899`).
6. Link "§ …, no Glossário" na Caderneta: alvo ~16px → padding.
7. Monólogo/epílogo 16px no celular → `text-lg` fixo (`MonologoFinal.jsx:64,99`).
8. NotaFrescor 12px e vozMestre da Caderneta 12px → `text-sm`; itálicos de 11px no mural → 12-14px.
9. Rótulo do relógio de bolso 8px → 10px (`RelogioBolso.jsx:12`).

## Decisões pendentes do usuário

> **Fechamento (13/07/2026, reescrita do caso):** a reescrita "A Hora Emprestada"
> resolveu por desenho as pendências **1** (o avistamento falso agora tem explicação
> plantada e paga no desfecho: a "luz do velho" era o lampião esquecido da oficina),
> **2** (três mentirosos inocentes, segredos de naturezas distintas), **3** (arquivada:
> `silenciamento` é motivo composto por natureza narrativa, único por id — o motor não
> mudou) e **5** (`ev_reacao_vital` no corpo, sinal modificador em jogo). A pendência
> **6** foi absorvida (rótulos novos dentro da norma, QA reescrito no mesmo commit).
> As pendências de UI (7, 8) e a cadeia da manhã (9) permanecem.

Ordenadas por impacto; nada disto foi aplicado à época (ver fechamento acima).

1. **Gale sem resolução (fair play b)** — `dep_avistamento_falso` é mentira literal sem
   explicação plantada de POR QUE ela jura ("viu-o à janela lá pelas oito" do dia 14).
   A KB de craft manda todo despiste ser fato verdadeiro com leitura errada. Opções:
   (a) Gale viu de fato um vulto à janela — Edgar às 9h encenando — e arredondou a hora
   (mexe em `horaAvistamentoDeclarada`/design); (b) plantar em prosa a razão da jura
   (vaidade de vizinha, confusão com outro dia). Recomendação: (a) na próxima iteração
   de design — transforma a isca em pista dupla, no espírito Obra Dinn do documento
   de conteúdo.
2. **Um só mentiroso inocente (fair play c)** — a KB pede ≥2 com segredos de naturezas
   distintas; Blackwood tem `segredo: null`. Atenuante: a régua de escalada legitima o
   tutorial no degrau baixo. Opções: exceção tutorial documentada OU segredo novo para
   Blackwood (mexe em seed/cartas). Recomendação: exceção documentada agora; segredo
   no primeiro caso da campanha.
3. **Herança × dívidas no motor** — hoje só `heranca` credita o móbil; `dep_dividas`
   (verdadeira, ligada a Edgar) custa a Vitória Absoluta. Aplicamos o plantio em prosa
   da hierarquia; a alternativa estrutural (motor aceitar motivo composto) mexe em
   `veredicto.js`/seed. Recomendação: avaliar motivo composto na campanha.
4. **Segredo de Hudson meio-resolvido** — o furto à meia-noite nunca é nomeado ao
   jogador (o monólogo diz só "encobria uma vergonha"). Nomear em carta/prosa ou
   registrar a meia-resolução como intencional.
5. **`reacao_vital` prometida pela KB, ausente do slice** — `asfixias.md`/`traumas.md`
   dizem que a reação vital no sulco prova o laço em vida; nenhuma carta carrega o
   sinal. Opções: carta/observação nova no corpo, ou atenuar as passagens da KB.
6. **Renomes estilísticos de rótulos contratados pelo QA** — "Xale de Lã Cinzenta com
   Fio Puxado" e "Edgar Saiu do Clube Antes das Nove" excedem a norma de 2–6 palavras,
   mas os renomes propostos quebram os regex do `qa-ui.mjs`. Renomear exige atualizar o
   QA no mesmo commit; não é erro grave, ficou adiado.
7. **UI [DECISÃO DO USUÁRIO] da KB — DECIDIDO em 19/07/2026 (usuário):**
   - (a) **Barbante clareado para `#c9553f`.** Aplicado: `stroke` do fio do mural em
     `MuralAcusacao.jsx` passa de `#a13b2e` para `#c9553f` — ganho de contraste sob
     protanopia sem perder o tom de lacre. (O `#a13b2e` do `.selo-cera` permanece.)
   - (d) **Alvo de toque de 44px — já cumprido.** `.opcao-dialogo` já tem
     `min-height: 44px` (o valor "~26px" do levantamento estava desatualizado). Sem ação.
   - (f) **Falas do legista em 14px itálico — já cumprido.** `FalaDoLegista`
     (`EventoLocalidade.jsx`) já renderiza `font-serif italic text-sm` (= 14px itálico).
     Ratificado. Sem ação.
   - (b) serifada 16px na prosa imersiva, (c) corpo 12px das cartas do mural e
     (e) claro-sobre-escuro vs "documento claro": **adiados para a sessão de UI/opções**
     (direção de leitura que rippla no jogo todo; decidir juntas, junto do futuro menu
     de opções).
8. **Convenção dos carimbos em notação moderna** ("20h–23h") — **DECIDIDO em 19/07/2026
   (usuário): registrar como convenção de UI da caderneta** (taquigrafia da interface, não
   prosa de época). **Hipótese registrada para troca:** rever para notação vitoriana
   quando existir um menu de opções e, sobretudo, quando entrar o pipeline de traduções.
9. **Cadeia da manhã apertada** — descoberta 9h30 → mensageiro → Caulfield → trem →
   cena às 11h em 1h30. Sem contradição numérica (Caulfield não tem distância fixada),
   mas o orçamento é justo. **DECIDIDO em 19/07/2026 (usuário): aceitar Caulfield como
   vila vizinha** (sem distância fixada, sem contradição). Sem edição de prosa.

### Notas (sem ação)

- Nomes "Geoffrey" (n. ~1832) e "Mabel" (n. ~1838) são revivals tardios improváveis
  para as idades — KB não proíbe; registrado para futuros elencos.
- Moldes do monólogo com corpo de caso ("o pescoço dizia outra coisa", "caiu pelo
  próprio relógio") — corretos no tutorial; parametrizar quando vier o procedural.
- Oportunidades de reforço mapeadas pela A1 (aplicar quando houver ordem): sino de
  recolha das 20h ancorando a ceia; faixa de crepe na cartola de Edgar (quem conhece a
  etiqueta lê a encenação do luto); escuridão de outubro como munição contra Pruitt;
  carteiro da manhã como testemunha natural contra Gale; ferrolho ausente na porta dos
  fundos reforçando a fragilidade da encenação.

## Fora de escopo (campanha)

- Rejogabilidade (seed fixa → mesma partida); variação por perito já mitiga no tutorial.
- Relógio que morde (pressão de rota real; janelas de disponibilidade).
- Endurecimento da retentativa em casos futuros.
- Modo procedural / arco mestre-aprendiz (`curriculo.js` e `derivarAparenciaDeSeed`
  seguem dormentes por desenho).
- Campo `custoTempo` dormente em `cartas.js` (dívida técnica registrada).
